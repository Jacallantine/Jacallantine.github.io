import "./date.css";

export const metadata = {
  title: "A Little Date? 💗",
  description: "A very important question for one very special girl.",
};

export default function DateLayout({ children }) {
  return children;
}
